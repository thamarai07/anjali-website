import {PlacementArray} from "../../utils/placement" 

export default function Placement() {
    console.log(PlacementArray)
 return (
   <div>
        <p className="text-[32px] font-semibold underline text-center">Our <span>Alumni</span> currently working at</p>
   </div>
 );
}